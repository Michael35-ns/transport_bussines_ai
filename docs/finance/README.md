# docs/finance

Cost model, documented financial formulas, KPI definitions, the direct vs.
indirect cost split, and the chosen indirect-cost allocation method (with the
alternatives that were considered).

Owned by `FinancialAnalystAgent` (`app/Ai/Agents/FinancialAnalystAgent.php`).
Phase: Financial model.
