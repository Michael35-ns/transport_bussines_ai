# docs/testing

Test strategy and QA plans, with explicit coverage of every critical financial
calculation, plus findings from the data audit.

Owned by `QaAgent` and `DataAuditorAgent` (`app/Ai/Agents/`).
Phase: QA → audit.
